{{- range .Values.services }}

{{- if .gateway.enabled }}

---
apiVersion: networking.istio.io/v1

kind: VirtualService

metadata:
  name: {{ .name }}
  namespace: {{ $.Values.namespace }}

spec:

  gateways:
    - {{ $.Release.Name }}-gateway

  hosts:
    - {{ .gateway.host }}

  http:

    - match:

        - uri:
            prefix: {{ .gateway.path | default "/" }}

      route:

        - destination:

            host: {{ .name }}

            port:
              number: {{ .service.port }}

{{- end }}

{{- end }}