{{- range .Values.repositories }}

apiVersion: v1

kind: Secret

metadata:

  name: repo-{{ .name }}

  namespace: {{ $.Values.namespace }}

  labels:

    argocd.argoproj.io/secret-type: repository

type: Opaque

stringData:

  type: git

  url: {{ .url }}

---

{{- end }}